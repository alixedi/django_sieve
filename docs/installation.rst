============
Installation
============

At the command line::

    $ easy_install django_sieve

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_sieve
    $ pip install django_sieve